# Migration Agent 项目入口（Agent 行为入口，非程序入口）

本文件是给 AI Agent 使用的项目入口说明。任何 Agent 接手本项目时，
必须先完整阅读本文件，再按需读取 `rules/` 与 `skills/` 中与当前任务
相关的文档。

注意：本文件不是程序运行入口，也不包含任何可执行逻辑。
程序的真实入口是根目录的 `main.py`。本文件只用来指导 AI Agent
理解项目结构、按需读取规则与技能，并遵守红线。

## 文件索引

| 路径 | 说明 | 何时读取 |
| --- | --- | --- |
| `AGENTS.md` | Agent 行为入口（非程序入口），只供 AI Agent 阅读 | 每次任务开始必读 |
| `main.py` | CLI 程序运行入口，接收两条路径 | 启动与入口相关 |
| `config.yaml` / `config.example.yaml` | 全局配置（本地文件/模板），LLM 切换 | 配置相关 |
| `agent/config.py` | 配置加载与校验 | 配置相关 |
| `agent/llm.py` | OpenAI 兼容 / Ollama 适配 | 模型相关 |
| `agent/guardrails.py` | 路径沙箱、白名单、预算、审批 | 任何改动前必读 |
| `agent/state.py` | 任务状态机与审计 | 流程相关 |
| `agent/planning.py` | 计划生成与校验 | 规划相关 |
| `agent/loop.py` | 主循环 | 流程相关 |
| `tools/` | 扫描、补丁、验证、报告 | 具体工具相关 |
| `retrieval/` | 文档导入、BM25、向量、重排 | 检索相关 |
| `migration/` | Python 2 到 3 基础迁移规则 | transform 相关 |
| `rules/` | 行为规则文档，索引见下方规则索引 | 必读 `00_总则.md`，其余按需 |
| `skills/` | 技能说明文档，索引见下方技能索引 | 按需 |
| `docs/` | 架构与设计文档 | 大改前按需 |
| `tests/` | 单元测试 | 任何代码改动后必跑 |

## 使用方式

1. 收到任务后先读 `AGENTS.md`；
2. 必读 `rules/00_总则.md`；
3. 涉及路径、工具、决策、验证时，读取 `rules/` 对应文档；
4. 涉及具体能力时，按需读取 `skills/` 对应文档；
5. 大型结构调整前，读取 `docs/01_总体架构.md`；
6. 代码改动后运行测试并保持全部通过。

## 规则索引

| 文件 | 内容 | 读取时机 |
| --- | --- | --- |
| `rules/00_总则.md` | 项目定位、适用范围、规则优先级、基本原则 | 每次任务必读 |
| `rules/01_路径与权限边界.md` | 输入只读、输出唯一可写、路径沙箱 | 涉及路径与文件操作时必读 |
| `rules/02_工具使用规范.md` | 工具白名单与调用约束 | 执行任何工具前必读 |
| `rules/03_迁移决策规范.md` | 证据要求、影响面分级、审批 | 生成或评审计划时必读 |
| `rules/04_验证与回滚规范.md` | 应用后验证、失败回滚 | 应用变更时必读 |
| `rules/05_审计与报告规范.md` | 审计目录、报告要求 | 任务收尾时必读 |

## 技能索引

| 文件 | 内容 | 调用场景 |
| --- | --- | --- |
| `skills/01_遗留代码扫描.md` | 扫描输入项目、识别遗留代码 | 需要扫描代码库时 |
| `skills/02_混合检索.md` | 文档导入、BM25/向量召回、Cross-Encoder 重排 | 需要检索迁移范例时 |
| `skills/03_迁移规划.md` | 生成并校验迁移计划 | 需要制定迁移方案时 |
| `skills/04_补丁应用.md` | 在输出目录应用计划条目 | 需要落地变更时 |
| `skills/05_验证与报告.md` | 验证输出并生成报告 | 需要收尾验证时 |

说明：技能文档后续随开发逐步落盘，索引先行声明；
新增、删除或重命名 `rules/` 或 `skills/` 文档时，必须同步更新本文件的索引。

## 红线（绝对不能违反）

1. 绝不修改输入项目路径内的任何文件，输入项目只读；
2. 绝不写入输出目录之外的任何路径；
3. 绝不执行工具白名单之外的 shell 命令或直接读写文件；
4. 绝不绕过 `PathGuard`、审批分级或预算限制；
5. 绝不在没有证据的情况下替换、删除或重写代码；
6. 绝不将验证失败的计划标记为 `applied`；
7. 绝不执行任何形式的系统级删除命令；
8. 绝不把 API Key 等敏感信息写入配置文件或提交；
9. 绝不跳过审计记录或伪造审计内容；
10. 绝不引入与代码实现冲突的规则表述。
11. 新增、删除或重命名 `rules/` 或 `skills/` 文档时，必须同步更新本文件索引。
12. 绝不把 `AGENTS.md` 当作程序入口执行，它只描述行为约束。

## 工作方式摘要

- 模型负责提出方案，harness 负责校验、执行与验证；
- `low` 影响自动应用，`medium` 与 `high` 必须人工审批；
- 每条计划应用后必须验证，验证失败立即回滚；
- 审计数据写入输出目录下的 `.migration-agent/`；
- 最终必须生成中文迁移报告。

## 测试命令

```bash
.venv\Scripts\python.exe -m unittest discover tests -v
```
